<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - PlantInformation</title>
    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" />
    <link rel="stylesheet" href="~/css/site.css" asp-append-version="true" />
    @* <link rel="stylesheet" href="~/PlantInformation/styles.css" asp-append-version="true" /> *@
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css" />
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/@@eonasdan/tempus-dominus@6/dist/css/tempus-dominus.min.css">
    @RenderSection("Styles", required: false)

</head>

<body>
    <header>
        <nav class="navbar-expand-sm navbar-light bg-white border-bottom box-shadow">
            <div class="container-header d-flex align-items-center justify-content-between">

                <!-- Logo + tên -->
                <a class="navbar-brand d-flex align-items-center" asp-area="" asp-page="/Index">
                    <i class="bi bi-leaf text-success me-2"></i>
                    <span class="fw-bold">Tra Cứu Cây Trồng</span>
                </a>

                <!-- Menu -->
                <div class="navbar-collapse collapse d-sm-inline-flex justify-content-between">
                    <ul class="navbar-nav ms-auto">
                    <li class="nav-item">
                            <a class="nav-link text-dark" asp-page="/Index">Trang chủ</a>
                        </li>
                         <li class="nav-item">
                            <a class="nav-link text-dark" asp-page="/LookUpPlant">Tra Cứu</a>
                        </li>
                    @if (User.IsInRole("Admin")) {
                         
                        <li class="nav-item">
                            <a class="nav-link text-dark" asp-page="/Admin/Dashboard">Quản lý cây trồng</a>
                        </li>
                    } else {
                   
                        <li class="nav-item">
                            <a class="nav-link text-dark" asp-page="/About">Về chúng tôi</a>
                        </li>

                    }
                        
                    </ul>

                    <!-- Thêm ms-auto ở đây để đẩy qua phải -->
                    <ul class="navbar-nav">
                        @if (User.Identity != null && User.Identity.IsAuthenticated)
                        {
                            // Kiểm tra vai trò
                            @if (User.IsInRole("Admin"))
                            {
                                <li class="nav-item dropdown">
                                    <a class="nav-link dropdown-toggle d-flex align-items-center" href="#" id="adminDropdown"
                                        role="button" data-bs-toggle="dropdown" aria-expanded="false">
                                        <i class="bi bi-person me-1"></i> @User.Identity.Name
                                    </a>
                                    <ul class="dropdown-menu dropdown-menu-end shadow" aria-labelledby="adminDropdown">
                                        <li>
    <a class="dropdown-item d-flex align-items-center" href="#" data-bs-toggle="modal" data-bs-target="#personalInfoModal">
        <i class="bi bi-person me-2"></i> Thông tin cá nhân
    </a>
</li>

                                        <li><a class="dropdown-item d-flex align-items-center" asp-page="/FavoritePlant"><i
                                                    class="bi bi-heart me-2"></i> Cây yêu thích</a></li>
                                        <li>
                                            <hr class="dropdown-divider">
                                        </li>
                                        <form method="post" asp-page="/Auth/Authentication" asp-page-handler="Logout"
                                            class="dropdown-item p-0">
                                            <button type="submit" class="btn btn-link dropdown-item d-flex align-items-center">
                                                <i class="bi bi-box-arrow-right me-2"></i> Đăng xuất
                                            </button>
                                        </form>
                                    </ul>
                                </li>
                            }
                            else
                            {
                                <li class="nav-item dropdown">
                                    <a class="nav-link dropdown-toggle d-flex align-items-center" href="#" id="userDropdown"
                                        role="button" data-bs-toggle="dropdown" aria-expanded="false">
                                        <i class="bi bi-person me-1"></i> @User.Identity.Name
                                    </a>
                                    <ul class="dropdown-menu dropdown-menu-end shadow" aria-labelledby="userDropdown">
                                        <li>
    <a class="dropdown-item d-flex align-items-center" href="#" data-bs-toggle="modal" data-bs-target="#personalInfoModal">
        <i class="bi bi-person me-2"></i> Thông tin cá nhân
    </a>
</li>

                                        <li><a class="dropdown-item d-flex align-items-center" asp-page="/FavoritePlant"><i
                                                    class="bi bi-heart me-2"></i> Cây yêu thích</a></li>
                                        <li>
                                            <hr class="dropdown-divider">
                                        </li>
                                        <form method="post" asp-page="/Auth/Authentication" asp-page-handler="Logout"
                                            class="dropdown-item p-0">
                                            <button type="submit" class="btn btn-link dropdown-item d-flex align-items-center">
                                                <i class="bi bi-box-arrow-right me-2"></i> Đăng xuất
                                            </button>
                                        </form>
                                    </ul>
                                </li>
                            }
                        }
                        else
                        {
                            <li class="nav-item">
                                <a class="nav-link" href="/Auth/Authentication">Login</a>
                            </li>
                        }
                    </ul>

                </div>

            </div>
        </nav>
    </header>
    <div class="container-body">
        <main role="main" class="pb-3">
        <partial name="_PersonalInfoPartial" model="new PlantManagement.DTOs.UpdateUserDTO()" />

            @RenderBody()
        </main>
    </div>
<footer>
    <div class="footer-container">
        <div class="footer-content">
            <!-- Về chúng tôi -->
            <div class="footer-section">
                <h4>Tra Cứu Cây Trồng</h4>
                <p>Cung cấp thông tin chi tiết về các loại cây trồng, hướng dẫn chăm sóc và kỹ thuật canh tác hiệu quả.</p>
                <div class="footer-social">
                    <a href="#" aria-label="Facebook"><i class="bi bi-facebook"></i></a>
                    <a href="#" aria-label="Instagram"><i class="bi bi-instagram"></i></a>
                    <a href="#" aria-label="YouTube"><i class="bi bi-youtube"></i></a>
                    <a href="#" aria-label="Email"><i class="bi bi-envelope"></i></a>
                </div>
            </div>

            <!-- Liên kết nhanh -->
            <div class="footer-section">
                <h4>Liên Kết Nhanh</h4>
                <ul>
                    <li><a asp-page="/Index"><i class="bi bi-chevron-right"></i>Trang chủ</a></li>
                    <li><a asp-page="/LookUpPlant"><i class="bi bi-chevron-right"></i>Tra cứu cây trồng</a></li>
                    <li><a asp-page="/About"><i class="bi bi-chevron-right"></i>Về chúng tôi</a></li>
                    @if (User.IsInRole("Admin"))
                    {
                        <li><a asp-page="/Admin/Dashboard"><i class="bi bi-chevron-right"></i>Quản lý</a></li>
                    }
                </ul>
            </div>

            <!-- Hỗ trợ -->
            <div class="footer-section">
                <h4>Hỗ Trợ</h4>
                <ul>
                    <li><a href="#"><i class="bi bi-chevron-right"></i>Câu hỏi thường gặp</a></li>
                    <li><a href="#"><i class="bi bi-chevron-right"></i>Hướng dẫn sử dụng</a></li>
                    <li><a asp-page="/Privacy"><i class="bi bi-chevron-right"></i>Chính sách bảo mật</a></li>
                    <li><a href="#"><i class="bi bi-chevron-right"></i>Điều khoản sử dụng</a></li>
                </ul>
            </div>

            <!-- Liên hệ -->
            <div class="footer-section">
                <h4>Liên Hệ</h4>
                <ul>
                    <li><i class="bi bi-geo-alt-fill"></i> Đà Nẵng, Việt Nam</li>
                    <li><i class="bi bi-telephone-fill"></i> +84 123 456 789</li>
                    <li><i class="bi bi-envelope-fill"></i> contact@tracuucaytrong.vn</li>
                    <li><i class="bi bi-clock-fill"></i> 24/7 Hỗ trợ trực tuyến</li>
                </ul>
            </div>
        </div>

        <div class="footer-bottom">
            <p>&copy; 2025 Tra Cứu Cây Trồng. All rights reserved. | Designed with <i class="bi bi-heart-fill text-danger"></i> for farmers</p>
        </div>
    </div>
</footer>
    <!-- Global Toast Notification -->
    <div class="toast-container-custom" id="toastContainer"></div>


    <partial name="_Chatbot" />

    <script src="~/lib/jquery/dist/jquery.min.js"></script>
    <script src="~/lib/bootstrap/dist/js/bootstrap.bundle.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/@@popperjs/core@2.11.8/dist/umd/popper.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/@@eonasdan/tempus-dominus@6/dist/js/tempus-dominus.min.js"></script>
    <script src="~/js/site.js" asp-append-version="true"></script>
   <script>
        function showToast(message, type = 'info') {
            const container = document.getElementById('toastContainer');
            
            // Title mapping - gán cứng theo type
            const titles = {
                success: 'Thành công!',
                warning: 'Cảnh báo!',
                danger: 'Lỗi!',
                info: 'Thông báo'
            };
            
            // Icon mapping
            const icons = {
                success: 'bi-check-circle-fill',
                warning: 'bi-exclamation-triangle-fill',
                danger: 'bi-x-circle-fill',
                info: 'bi-info-circle-fill'
            };

            const title = titles[type] || titles.info;

            // Create toast element
            const toast = document.createElement('div');
            toast.className = `toast-custom toast-${type}`;
            toast.innerHTML = `
                <div class="toast-icon">
                    <i class="bi ${icons[type]}"></i>
                </div>
                <div class="toast-content">
                    <div class="toast-title">${title}</div>
                    <div class="toast-message">${message}</div>
                </div>
                <button class="toast-close" onclick="closeToast(this)">
                    <i class="bi bi-x"></i>
                </button>
                <div class="toast-progress"></div>
            `;

            container.appendChild(toast);

            // Auto remove after 5 seconds
            setTimeout(() => {
                closeToast(toast.querySelector('.toast-close'));
            }, 5000);
        }

        function closeToast(button) {
            const toast = button.closest('.toast-custom');
            toast.classList.add('hiding');
            setTimeout(() => {
                toast.remove();
            }, 300);
        }

        @if (TempData["ToastMessage"] != null)
            {
                <text>
                    document.addEventListener("DOMContentLoaded", function () {
                        showToast("@Html.Raw(TempData["ToastMessage"])", "@TempData["ToastType"]");
                });
                </text>
        }
    </script>


      <script src="https://cdnjs.cloudflare.com/ajax/libs/microsoft-signalr/7.0.5/signalr.min.js"></script>
    <script>
        const connection = new signalR.HubConnectionBuilder()
            .withUrl("/lockUserHub")
            .build();

        connection.on("Locked", function(message) {
            alert(message);
            console.log("Received locked event", message); 
            window.location.href = "/Auth/Authentication?locked=true";
        });

        connection.start().catch(function(err) {
            return console.error(err.toString());
        });
    </script>
    


    @await RenderSectionAsync(name: "Scripts", required: false)
</body>

</html>